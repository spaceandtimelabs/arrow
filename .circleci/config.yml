# CircleCI configuration file
version: 2.1

orbs:
  maven: circleci/maven@1.3.0
  artifactory: jfrog/artifactory-orb@1.0.1

jobs:
  tag:
    executor: maven/default
    working_directory: ~/arrow
    steps:
      - checkout
      - run:
          name: Create Tag
          command: git tag circle-$CIRCLE_BUILD_NUM
      - run:
          name: Push Tag
          command: git push origin --tags
  deploy_submodule:
    executor: maven/default
    working_directory: ~/arrow
    parameters:
      submodule:
        type: string
      extra_parameters:
        type: string
    steps:
      - checkout
      - artifactory/install
      - artifactory/configure
      - run:
          name: Submodule Name
          command: echo << parameters.submodule >>
      - restore_cache:
          keys:
            - << parameters.submodule >>-dependencies-{{ checksum "java/pom.xml" }}
            - << parameters.submodule >>-dependencies-
      - run:
          name: Build Package
          command: |
            jfrog rt mvn -pl ':<< parameters.submodule >>' clean install --update-snapshots -f java/pom.xml << parameters.extra_parameters >>;
      - artifactory/build-integration
      - store_test_results:
          path: ${basedir}/target/surefire-reports
      - save_cache:
          paths:
            - ~/.m2
          key: << parameters.submodule >>-dependencies-{{ checksum "java/pom.xml" }}

workflows:
  main_workflow:
    jobs:
      - tag:
          name: Tag Build
      - deploy_submodule:
          name: Build Arrow Format JAR
          submodule: arrow-format
          extra_parameters: "-Drat.skip=true -DskipTests=true -Dmaven.javadoc.skip=true -Dcheckstyle.skip=true"
          context: Artifactory
          requires:
            - Tag Build
      - deploy_submodule:
          name: Build Arrow Memory JAR
          submodule: arrow-memory
          extra_parameters: "-Drat.skip=true -DskipTests=true -Dmaven.javadoc.skip=true -Dcheckstyle.skip=true"
          context: Artifactory
          requires:
            - Tag Build
      - deploy_submodule:
          name: Build Arrow Vector JAR
          submodule: arrow-vector
          extra_parameters: "-Drat.skip=true -DskipTests=true -Dmaven.javadoc.skip=true -Dcheckstyle.skip=true"
          context: Artifactory
          requires:
            - Build Arrow Format JAR
            - Build Arrow Memory JAR
      - deploy_submodule:
          name: Build Arrow Compression JAR
          submodule: arrow-compression
          extra_parameters: "-Drat.skip=true -DskipTests=true -Dmaven.javadoc.skip=true -Dcheckstyle.skip=true"
          context: Artifactory
          requires:
            - Build Arrow Vector JAR
      - deploy_submodule:
          name: Build Arrow JDBC JAR
          submodule: arrow-jdbc
          extra_parameters: "-Drat.skip=true -DskipTests=true -Dmaven.javadoc.skip=true -Dcheckstyle.skip=true"
          context: Artifactory
          requires:
            - Build Arrow Vector JAR
      - deploy_submodule:
          name: Build Arrow Plasma JAR
          submodule: arrow-plasma
          extra_parameters: "-Drat.skip=true -DskipTests=true -Dmaven.javadoc.skip=true -Dcheckstyle.skip=true"
          context: Artifactory
          requires:
            - Build Arrow Vector JAR
      - deploy_submodule:
          name: Build Arrow Flight JAR
          submodule: arrow-flight
          extra_parameters: "-Drat.skip=true -DskipTests=true -Dmaven.javadoc.skip=true -Dcheckstyle.skip=true"
          context: Artifactory
          requires:
            - Build Arrow Java Root JAR
      - deploy_submodule:
          name: Build Flight Core JAR
          submodule: flight-core
          extra_parameters: "-Drat.skip=true -DskipTests=true -Dmaven.javadoc.skip=true -Dcheckstyle.skip=true"
          context: Artifactory
          requires:
            - Build Arrow Vector JAR
            - Build Arrow Flight JAR
      - deploy_submodule:
          name: Build Flight SQL JAR
          submodule: flight-sql
          extra_parameters: "-Drat.skip=true -DskipTests=true -Dmaven.javadoc.skip=true -Dcheckstyle.skip=true"
          context: Artifactory
          requires:
            - Build Flight Core JAR
      - deploy_submodule:
          name: Build Flight JDBC Driver JAR
          submodule: flight-jdbc-driver
          extra_parameters: "-Drat.skip=true -Dmaven.test.skip=true -Dmaven.javadoc.skip=true -Dcheckstyle.skip=true -Dmaven.site.skip=true"
          context: Artifactory
          requires:
            - Build Flight SQL JAR
            - Build Arrow JDBC JAR
      - artifactory/docker-publish:
          docker-registry: spaceandtime.jfrog.io/sxt-docker-docker
          docker-steps:
            - restore_cache:
                keys:
                  - sqlline-dockerfile-build-dependencies-{{ checksum "java/pom.xml" }}
                  - sqlline-dockerfile-build-dependencies-
            - run:
                name: Download Assembly
                command: |
                  jfrog rt mvn -pl ':flight-jdbc-driver' clean install -f java/pom.xml --update-snapshots -Dcheckstyle.skip=true -Dmaven.site.skip=true -Drat.skip=true -Dmaven.test.skip=true -Dmaven.javadoc.skip=true;
            - run:
                name: Build Image
                command: |
                  cd java/flight/flight-jdbc-driver
                  docker build -f sqlline.Dockerfile . -t $DOCKERTAG
            - artifactory/build-integration
            - save_cache:
                paths:
                  - ~/.m2
                key: sqlline-dockerfile-build-dependencies-{{ checksum "java/pom.xml" }}
          docker-tag: >-
            spaceandtime.jfrog.io/sxt-docker-docker/ballista-sqlline:$CIRCLE_BUILD_NUM
          name: Build Flight SQLLine Docker Image
          repository: sxt-docker-docker
          context: Artifactory
          requires:
            - Build Flight JDBC Driver JAR